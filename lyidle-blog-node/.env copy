# 用于控制是否是生产模式 验证码 apifox 发送邮箱的验证码方便提取 访客信息模拟 设置信息的模拟 redis 的连接有无密码
is_production=false
# api  的前缀
api_prefix='/api'
# api 端口
api_port=3594
# web 端口
web_port=3598

# 默认用户 的角色信息
default_user='["user"]'
# 默认网站拥有者 的角色信息
default_owner='["owner"]'
# 默认管理员 的角色信息
default_admin='["admin"]'

# 初始化生成角色 的种子文件需要的权限
seeder_roles='["owner","admin", "user","doc"]'
# 初始化生成权限组 的种子文件需要的权限
seeder_permission_group='["user","menu","doc"]'
# 初始化生成权限 的种子文件需要的权限
seeder_permissions='["user:read","user:write","menu:read","menu:write","doc:publish"]'
# 初始化生成的 owner 的用户组
seeder_owner_group='["user","menu","doc"]'
# 初始化生成的 admin 的用户组
seeder_admin_group='["user","menu","doc"]'
# 初始化生成的 user 的用户组
seeder_user_group='["user"]'

# 生成jwt 的 hash
jwt_hash=9&fQkK$a^o!rNq*8ra&F2ot!S@b@3ekw72q
# 邮箱服务
email_send=false
email_host=smtp.office365.com
email_port=587
email_secure=false
email_requireTLS=true
email_user=
email_pwd=
# 注册邮箱的平台
email_reg_stage=Lyのblog
# redis 
redis_prefix=vite-blog # redis 的保存键的 前缀
redis_pwd=redis_bXwWeD
redis_host=154.9.253.229
# 使用 ms 库进行转换成对应的毫秒
# 时间的单位 h、m、s、y、d对应时、分、秒、年、日
# 验证码有效期
code_expire=1m #按照秒
# 软删除文章的时间
delete_article_expire=5s
# 软删除用户的时间
delete_user_expire=10s
# 软删除菜单的时间
delete_menu=10s
# token过期时间
token_expire=5h
# 实时性不强的数据 的缓存时间 如短诗 第三方接口
default_expire=1d
# 压缩
gifsicle_route_window='C:\\My_Program_Folder\\Environments\\gifsicle-1.95-win64\\gifsicle.exe'